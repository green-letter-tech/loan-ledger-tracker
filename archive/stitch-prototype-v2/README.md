# Stitch prototype v2 (archived)

Vite + React + Tailwind UI exported from Google Stitch.

**Superseded by:** `Lend Ledger/` at repo root.

To run this old prototype (reference only):

```bash
npm install
npm run dev
```
